packages:
  - '.'
allowBuilds:
  esbuild: true

overrides:
  form-data: 4.0.5

# Fail installation when a newly introduced dependency requires an
# unreviewed build script, instead of silently skipping it.
strictDepBuilds: true
