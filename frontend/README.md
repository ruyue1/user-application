# Base 前端模板

本工程提供 React、Ant Design、公共请求封装、基础布局、页面发现机制和默认欢迎页。默认入口页面为 `/page/welcome`。

## 开发与验证

```bash
pnpm install
pnpm dev
pnpm build
pnpm test
```

新增业务页面和代码放置约定见 [项目结构文档](docs/project-structure.md)。

启用的 Capability 会同时生成其前端说明至 `docs/capabilities/`；该目录只反映当前实际启用的能力。
